kind: Service
apiVersion: v1
metadata:
  name: payment
  namespace: roboshop
spec:
  selector: # labels are used as selectors
    app: payment
    tier: app
    project: roboshop
      
  ports:
  - protocol: TCP
    port: 8080 # service port
    targetPort: 8080